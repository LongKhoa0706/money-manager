import 'package:flutter/material.dart';

class HistoryTab extends StatelessWidget {
  /// Create a HistoryTab widget.
  HistoryTab({
    Key key,
  }): super(key: key);

  /// Build this widget.
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('History'),
      ),
    );
  }
}
